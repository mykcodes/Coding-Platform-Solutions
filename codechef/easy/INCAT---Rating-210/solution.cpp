#include <bits/stdc++.h>
using namespace std;

int main() {
    string s,X;
    cin>>s;
    X="act";
    sort(s.begin(),s.end());
    (s==X)
    ? cout<<"Yes"
    : cout<<"No";
    
    return 0;
}
